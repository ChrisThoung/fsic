language: python
before_install:
  - sudo apt update
  - sudo apt install gfortran
python:
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - pip install networkx
  - pip install numpy
  - pip install pandas
  - pip install sympy
  - pip install pytest
  - python setup.py install
script:
  - pytest -v
